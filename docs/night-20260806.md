# Projects Night — 2026-08-06

Autonomous session (Claude Fable). Goal: assess both repos, fix obvious bugs, ship one
delight feature, leave everything green and pushed.

## Assessment

Started from PR #14's `NEXT-STEPS.md` review (written at v1.0.93, repo now at v1.0.102).
Most of its P0 list had already landed since: the tier-2+ crafting namespace bug, the
server-side LLM spend gate, save retries, ore-chance dead config, and the ~17.7K-line
dead-code sweep are all fixed on `main`. The Wilds countdown + fresh-ore banner the
review asked for also already exists (`WildsResetController.luau`) — that section of the
review is stale. `origami-server` is healthy (BYOM hardened, dual-provider link merged);
its only open item is stale PR #1 (catalog + Kimi cleanup), untouched tonight.

## Shipped (all on `main`, each pushed individually)

1. **`4e060fc` — publish pipeline now gated on CI.** `publish.yml` ran lint/format/tests
   with `continue-on-error: true` everywhere, so broken code shipped to Roblox. Removed
   the escapes; `publish` now `needs: lint-and-test` (NEXT-STEPS P0 #4).
2. **`26319ef` — console can craft again.** All 18 `MouseButton1Click` handlers in
   BackpackController + WorkshopTab → `.Activated` (gamepad confirm never fired them;
   NEXT-STEPS #14).
3. **`2a2beae` — mobile can open the shop.** The shop was reachable only via keyboard B /
   gamepad Y — touch players had literally no way in (NEXT-STEPS #15). Added an
   always-visible 🛒 toggle button below the 📦 Backpack button.
4. **`475d517` — delight feature: Expedition Records.** New "Stats" tab in the Backpack
   (GDD "records & discovery", NEXT-STEPS P3 #16): blocks mined, deepest layer, layers
   explored (N of 7), items folded, time exploring, expeditions, gifts given, plus a
   "Next stop: <layer>" teaser. Reads only already-persisted PlayerData — zero new server
   tracking. Pure logic in require-free `Shared/ExpeditionRecords.luau` with 15 Lune tests
   (layer names/boundaries injected from `BiomeConfig` to keep one source of truth).
5. **`ee7f53d` — CI incident found & fixed: red since July 15.** Every `loadstring`-based
   spec fails under the aftman-pinned lune 0.8.9 (no `loadstring` global). Local runs
   passed only because Homebrew's lune 0.10.4 shadows the aftman shim on PATH — so nobody
   noticed for three weeks. Pinned lune 0.10.4 (suite passes 87/87 under the shim) and
   updated the CLAUDE.md toolchain table. With #1 above, this also unblocks publishing.
   Also `7fffcd6` (codemap regen — it counts `git ls-files`, so regenerating before
   staging new files produces a stale map; regenerate after `git add`).

Housekeeping: closed stale playground PR #10 (superseded by merged #13).

## Verification

`selene src` clean, `stylua --check` clean, `lune run tests/runner` 87/87 (run under the
aftman-pinned lune, not PATH lune — that distinction is the whole CI incident), and
`rojo build default.project.json` succeeds. Player-in-Studio verification of the two UI
changes wasn't possible headlessly; the Records tab reuses the existing tab/card
patterns and the risky part (UIListLayout leaking into the shared content frame — it's
not a GuiObject, so `clearChildren` misses it) is wrapped in a container frame.

## Needs Mark

- **API key still committed** (`src/ServerStorage/Configuration/Server.luau`, P0 #3).
  The fix needs a Roblox Creator Hub secret (`HttpService:GetSecret`) + another rotation
  — account access required, so not attempted autonomously.
- **PR #14** (NEXT-STEPS review) left open per its "not intended for auto-merge" note.
  Several sections are now stale (see above) — worth a skim-and-merge or close.
- Quick Studio sanity pass on: Backpack Stats tab, 🛒 button placement, gamepad crafting.
- Next highest-leverage per the review: P1 items — real Browse/Catalog (recipes, no
  per-summon LLM call), Free Creates + Robux SKUs, onboarding hook.
